- name: Install mongodb componet
  hosts: mongodb
  tasks:
  - name: copy mango.repo to yum.repos.d
    ansible.builtin.copy:
      src: mongo.repo
      dest: /etc/yum.repos.d/mongo.repo
  - name: Install Mongodb
    ansible.builtin.yum:
      name: mongodb-org
      state: installed